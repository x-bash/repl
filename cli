
___x_cmd_repl_cli(){

    local NAME="${1:?Provide name}"

    (cat "$TMPDIR/stdout.${NAME}") >&1 &
    (cat "$TMPDIR/stderr.${NAME}") >&2 &

    while read -r line; do
        printf "%s\n" "$line" >"$TMPDIR/stdin.${NAME}"
    done

}

___x_cmd_repl_cli "$@"
